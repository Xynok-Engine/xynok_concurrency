[package]
name = "xynok-async-lessons"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
tokio = { version = "=1.53.1", features = ["rt-multi-thread", "macros", "time", "net", "io-util", "sync", "fs"] }
